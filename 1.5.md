## 1.5
~~~
#зависает потому что перегружается очень сильно
z = 1
z <<= 40  # z = 2^40
print("z =", z)

# Вместо 2 ** z используем логарифм
import math
log_value = z * math.log(2)
print("Логарифм 2^z:", log_value)
